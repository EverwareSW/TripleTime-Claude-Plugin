---
description: Alias for /tripletime - control TripleTime time tracking
argument-hint: <subcommand> [args...]
---

This is an alias for `/tripletime`. Read the full instructions from `${CLAUDE_PLUGIN_ROOT}/commands/tripletime.md` (or, if `CLAUDE_PLUGIN_ROOT` is unset, from this plugin's `commands/tripletime.md` next to this file) and follow them with `$ARGUMENTS`.
